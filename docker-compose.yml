services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile.dev
    volumes:
      - ./:/app
      - /app/node_modules
    ports:
      - "5173:5173"
    environment:
      VITE_BASE_URL_API_CLIENT: ${VITE_BASE_URL_API_CLIENT:-http://localhost:3000}
      VITE_CELLPHONE_NUMBER: ${VITE_CELLPHONE_NUMBER:-5571996063783}
      VITE_LINKEDIN_ACCOUNT: ${VITE_LINKEDIN_ACCOUNT:-algeferson}
